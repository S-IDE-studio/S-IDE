{
  "name": "side-mobile",
  "version": "1.0.0",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "build": "eas build --platform all --profile production --non-interactive",
    "build:ios": "eas build --platform ios --profile production --non-interactive",
    "build:android": "eas build --platform android --profile production --non-interactive",
    "build:preview": "eas build --platform all --profile preview --non-interactive",
    "submit": "eas submit --platform all --profile production --non-interactive"
  },
  "dependencies": {
    "@side-ide/shared": "file:../../packages/shared",
    "expo": "~52.0.0",
    "expo-status-bar": "~2.0.0",
    "react": "18.3.1",
    "react-native": "0.76.5",
    "react-native-safe-area-context": "4.12.0",
    "react-native-screens": "~4.1.0",
    "@react-navigation/native": "^6.1.0",
    "@react-navigation/native-stack": "^6.9.0"
  },
  "devDependencies": {
    "@babel/core": "^7.25.0",
    "@types/react": "~18.3.12",
    "eas-cli": "^10.0.0",
    "typescript": "^5.3.0"
  },
  "private": true
}
